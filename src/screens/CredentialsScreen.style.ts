import { colors } from '../colors'

export const style = {
  credentialInputStyle: {
    fontSize: 16,
    height: 40,
    borderColor: colors.black,
    borderWidth: 1,
    paddingLeft: 4,
    borderLeftWidth: 0,
    borderRightWidth: 0,
    borderTopWidth: 0,
  }
}
